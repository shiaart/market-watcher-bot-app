{
  "name": "market-watcher-bot-app",
  "version": "0.1.0",
  "description": "",
  "scripts": {
    "start": "npm run serve",
    "dev":  "npm run build && NODE_ENV=dev node dist/bot.js",
    "serve": "NODE_ENV=production node dist/bot.js",
    "build": "npm run build-ts && npm run tslint",
    "build-ts": "tsc",
    "tslint": "tslint -c tslint.json -p tsconfig.json",
    "debug": "npm run build && npm run watch-debug"
  },
  "dependencies": {
    "axios": "^0.19.2",
    "dotenv": "^8.2.0",
    "https-proxy-agent": "^5.0.0",
    "node-schedule": "^1.3.2",
    "telegraf": "^3.38.0",
    "telegraf-i18n": "^6.6.0",
    "tslint-plugin-prettier": "^2.3.0"
  },
  "devDependencies": {
    "@types/node": "^14.0.5",
    "@types/node-schedule": "^1.3.0",
    "tslint": "^6.1.2",
    "tslint-config-prettier": "^1.18.0",
    "typescript": "^3.9.3"
  }
}
