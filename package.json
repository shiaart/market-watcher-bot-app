{
  "name": "market-watcher-bot-app",
  "version": "0.1.0",
  "description": "",
  "scripts": {
    "start": "npm run serve",
    "serve": "set NODE_ENV=production&&node dist/bot.js",
    "build": "npm run build-ts && npm run tslint",
    "build-ts": "tsc",
    "tslint": "tslint -c tslint.json -p tsconfig.json",
    "debug": "npm run build && npm run watch-debug"
  },
  "dependencies": {
    "dotenv": "^8.2.0",
    "https-proxy-agent": "^5.0.0",
    "telegraf": "^3.38.0",
    "telegraf-i18n": "^6.6.0"
  },
  "devDependencies": {
    "@types/node": "^14.0.5",
    "tslint": "^6.1.2",
    "typescript": "^3.9.3"
  }
}
